/**
 * Core Module Exports
 *
 * Centralized exports for core modules (Store, BaseModal)
 */

const { Store, store } = require('./store');
const { BaseModal } = require('./BaseModal');

module.exports = {
  Store,
  store,
  BaseModal
};
